<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Foreground keep-alive service (Task 5.2). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- Auto-start on boot (Task 5.3). -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Low-importance foreground notification vibration (optional but declared). -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:label="message_assistant"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Disable Impeller, use the classic Skia renderer. Impeller (the
             default since Flutter 3.16) uses Vulkan on Android and on some
             Mali/GLES stacks it stalls the raster thread when the Activity's
             Surface is obscured while an indeterminate animation keeps
             requesting frames (exactly the RPA auto-reply case: the chain runs
             while the user is backgrounded in WeChat). The stall leaves a
             permanent black screen on resume. Skia (the previous renderer) does
             not reproduce this. This is Flutter's recommended device-specific
             mitigation: https://github.com/flutter/flutter/issues?q=impeller+black
             -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- Task 5.2 — catches posted notifications (WeChat), parses + streams to Flutter.
             Requires user-granted Notification Listener access (Settings). -->
        <service
            android:name=".MessageNotificationListenerService"
            android:exported="false"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Task 5.2 — keep-alive foreground service. specialUse type on API 34+
             (we are not media/location/data-sync/etc.), declared via the property. -->
        <service
            android:name=".MonitorForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="notification_monitoring" />
        </service>

        <!-- Task 5.3 — auto-start on device boot. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Android 11+ package visibility: declare WeChat so getLaunchIntentForPackage
             returns non-null on API 30+. Required for the "回复 → 唤起微信" flow. -->
        <package android:name="com.tencent.mm"/>
    </queries>
</manifest>
